# Both values are safe to commit — the publishable key is public by design and
# every table is protected by row level security. Defaults are baked into the
# code, so you only need this file to point the app at a different project.
VITE_SUPABASE_URL=https://fowtgumnirjklfdonddk.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RgDKZMbt_347SjM5NuGapg_coAQlpF7
